{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha **/**/*.test.js --timeout 10000",
    "start": "node server/server.js",
    "test-watch": "nodemon --exec \"npm test\""
  },
  "author": "Daniel Marcano",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "socket.io": "^2.1.0"
  }
}
